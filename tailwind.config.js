/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        'brand-primary': '#5A3964',      // Example color, adjust as needed
        'brand-secondary': '#A67EB8',    // Example color, adjust as needed
        'text-secondary': '#E5D9F2',     // Example color, adjust as needed
        'text-primary': '#22223B',       // Example color, adjust as needed
        // Add other custom colors here
      },
    },
  },
  plugins: [],
}